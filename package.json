{
  "name": "litflow-ai",
  "private": true,
  "version": "0.1.0",
  "description": "Anna App: an AI-native research copilot that turns a topic into a structured literature review focused on research gaps. Reaches the model via anna.llm.complete.",
  "scripts": {
    "dev": "anna-app dev --slug litflow-ai",
    "dev:mock": "anna-app dev --slug litflow-ai --mock-llm fixtures/happy-path.jsonl",
    "dev:off": "anna-app dev --slug litflow-ai --no-llm",
    "validate": "anna-app validate --strict",
    "push": "anna-app apps push",
    "cut": "anna-app apps cut 0.1.0"
  },
  "devDependencies": {
    "@anna-ai/cli": "^0.1.30"
  }
}
